#include<stdio.h>
#define pi 3.14159
#define area_circle(x) (pi)*(x)*(x)

int main(){
	float x,;
	printf("Enter the Radius of the circle :");
	scanf("%f",&x);
	printf("The Area of the Circle is %.2f\n",area_circle(x));
	return 0;
	}
